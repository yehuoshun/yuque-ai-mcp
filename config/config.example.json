{
  "token": "在此填入语雀 API Token",
  "api_base": "https://www.yuque.com/api/v2",
  "cookie": "可选，回收站/上传功能需要。浏览器登录后 F12 → Application → Cookies → 复制完整 Cookie 字符串",
  "ctoken": "可选，从 Cookie 中提取 yuque_ctoken 的值",
  "rss": {
    "enabled": true,
    "namespaces": {
      "cnblogs": {
        "book_id": "可选，博客园 RSS 知识库数字 ID",
        "namespace": "可选，知识库 namespace，如 my-group/my-repo"
      }
    }
  },
  "kv": {
    "enabled": false,
    "namespaces": {
      "cnblogs": {
        "book_id": "可选，KV 去重知识库数字 ID（enabled=true 时必填）",
        "docs": ["可选，分片文档的 doc_id 数组"]
      }
    }
  },
  "crawler": {
    "enabled": true,
    "namespaces": {
      "cnblogs": {
        "book_id": "可选，爬虫目标知识库数字 ID",
        "namespace": "可选，知识库 namespace，如 my-group/my-repo"
      }
    }
  }
}