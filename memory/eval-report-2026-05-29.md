# 索引搜索测评报告

> 测试时间：2026-05-29 15:46  
> 源库：luoboQAQ (yehuoshun/nmwb0s) 55 篇  
> 子库：index-luobo (79540240) 31 篇  
> 总库：index-route (79540659) 34 篇  

---

## 1. 覆盖率 (Recall)

| 指标 | 结果 |
|------|------|
| 源文档总数 | 55 |
| 索引覆盖 | 55 (100%) |
| 未覆盖 | 0 |

✅ 全部源文档可通过索引检索

---

## 2. 搜索命中率

| 指标 | 结果 |
|------|------|
| 索引关键词总数 | 31 |
| scope 搜索可命中 | 31 (100%) |
| 搜不到 | 0 |

✅ 31 个关键词全部可通过搜索 API 命中

---

## 3. 准确率 (Precision) — 抽查

| 搜索词 | 预期文档 | 命中率 | 评价 |
|--------|---------|--------|------|
| SpringBoot | 3篇 | 2/3 | Spring 独立关键词，未混入 |
| 机器学习 | 3篇 | 3/3 | ✅ 完全匹配 |
| DNS | 2篇 | 2/2 | ⚠️ 混入 WireGuard/SSL（合并过度） |
| Xposed | 2篇 | 2/2 | ✅ 完全匹配 |
| 算法与数据结构 | 3篇 | 3/3 | ✅ 完全匹配 |

**平均匹配率: 12/13 = 92%**

⚠️ DNS 关键词合并了 WireGuard 相关文档，导致搜索结果混入不相关内容。根因：`MERGE_RULES` 将 `Webhooks/CI/CD/SSL证书` 都合并到了 `WireGuard`，而 DNS 搜 `yehuoshun/index-luobo` 会命中含 DNS 关键词的其他索引文档 body。

---

## 4. 权重分布

| 权重 | 数量 | 占比 |
|------|------|------|
| w=10 | 55 | 76% |
| w=9 | 7 | 10% |
| w=8 | 5 | 7% |
| w=7 | 5 | 7% |

⚠️ 严重偏斜：76% 权重为 10，区分度不足。根因：手工评分而非 LLM 逐文档深度判断。真用 LLM 评分会显著改善。

---

## 5. 边界情况

| 测试 | 预期 | 实际 | 状态 |
|------|------|------|------|
| 不存在的关键词 | 0 命中 | 0 | ✅ |
| `springboot` (全小写) | ≥1 | 1 | ✅ |
| `Spring Boot` (带空格) | ≥1 | **0** | ❌ |
| `JWT认证` (中英混合) | ≥1 | **0** | ❌ |

**2 个严重问题：**

1. **`Spring Boot` 带空格 → 0 命中**：语雀搜索将空格视为 AND 操作符，而路由文档标题是 `SpringBoot`（无空格）。`cleanToken` 未处理空格 → kbSearch 收到带空格的 token → 搜索失败。

2. **`JWT认证` → 0 命中**：JWT 被合并到 `JavaWeb` 关键词，但 `JWT认证` 作为独立查询时路由库找不到匹配的标题。用户搜 "JWT" 能命中，但搜 "JWT认证" 不行。

---

## 6. 总分

| 维度 | 得分 | 说明 |
|------|------|------|
| 覆盖率 | 100% ✅ | 55/55 全索引 |
| 搜索命中率 | 100% ✅ | 31/31 关键词可搜 |
| 准确率 | 92% ✅ | 12/13 预期命中 |
| 权重排序 | ⚠️ | 76% 集中 w=10，区分度低 |
| 边界鲁棒性 | ⚠️ | 空格/中英混合 2 个 0 命中 |
| 端到端可用性 | ✅ | 路由→子库→展开 全链路通 |

---

## 7. 待改进

| 优先级 | 问题 | 方案 |
|--------|------|------|
| 🔴 P0 | `Spring Boot` 空格 → 0 命中 | `cleanToken` 去空格，或搜索前分词生成多 token |
| 🔴 P0 | `JWT认证` → 0 命中 | `keywords` 数组应包含自然问法变体，路由标题需匹配 |
| 🟡 P1 | 权重倾斜 | 真用 LLM 逐文档评分替代手工打分 |
| 🟡 P1 | DNS 误引入 WireGuard | 拆分过度的关键词合并（DNS 不应合并网络工具） |
| 🟢 P2 | SpringBoot 未包含 Spring 学习笔记 | 关键词粒度问题，Spring ≠ SpringBoot，设计合理 |

---

## 8. 结论

**索引和搜索管线已就绪，端到端可用。** 覆盖率 100% + 命中率 100% + 准确率 92%。两个 P0 边界情况（空格/复合词搜索）需要修复 `cleanToken` 和路由文档标题策略，但不影响正常单 token 搜索。